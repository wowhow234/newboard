import React, { useState } from "react";

const Pagination = ({ boardsNum, startPage, boardPerPage }) => {
  // 게시글 전체 갯수 : boardNum
  // 시작페이지 : startPage
  // 한 페이지당 게시글 갯수 : boardPerPage

  // 전체 페이지 수
  // const totalPage =

  return <div style={{ border: "1px solid black" }}>페이지네이션</div>;
};

export default Pagination;
